<div class="booyah-box col-xs-10 col-xs-offset-1">
  <h2>Sign up</h2>

  <br />
  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :email, required: true, autofocus: true %>
      <%= f.input :password, required: true, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length) %>
      <%= f.input :password_confirmation, required: true %>
    </div>
    <br />
    <div class="form-actions">
      <%= f.button :submit, "Sign up", :class => 'btn btn-primary' %>
    </div>
  <% end %>
  <br />
  <%= render "devise/shared/links" %>
</div>

<div style="clear:both;"></div>
<br />

<div class="col-xs-10 col-xs-offset-1" style="background-color:whitesmoke; padding:20px; border: 1px solid gray;">
  <h3>Want to assess your entire organization?</h3>
  <br />
  <p>If you are looking to assess your entire organization's level of Total Customer Focus, <%= link_to 'register here', new_admin_registration_path %> for an admin account. You can invite your colleagues to take the assessment and analyze your organization's results.</p>
</div>